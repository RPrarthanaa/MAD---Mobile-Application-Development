<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Connect4App"
        tools:targetApi="31">
        <activity
            android:name=".Main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ModeSelection.VsPlayerSelectionActivity"/>
        <activity android:name=".ModeSelection.VsAISelectionActivity"/>
        <activity android:name=".CustomizeUser.CustomizeUser1"/>
        <activity android:name=".CustomizeUser.CustomizeUser2"/>
        <activity android:name=".CustomizeGame.CustomizeGameSettings"/>
        <activity android:name=".GameBoard.GameplayActivity"/>
        <activity android:name=".GameBoard.GameStatisticsActivity"/>
        <activity android:name=".CustomizeGame.CustomizeGameAiSettings"/>
    </application>
</manifest>